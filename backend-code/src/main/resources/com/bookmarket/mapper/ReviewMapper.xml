<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookmarket.mapper.ReviewMapper">

    <resultMap id="BaseResultMap" type="com.bookmarket.pojo.Review">
            <id property="id" column="id" />
            <result property="bookId" column="book_id" />
            <result property="userId" column="user_id" />
            <result property="userName" column="user_name" />
            <result property="userAvatar" column="user_avatar" />
            <result property="rating" column="rating" />
            <result property="content" column="content" />
            <result property="createdAt" column="created_at" />
    </resultMap>
    <select id="getReviewsByBookId" resultType="com.bookmarket.pojo.Review">
        select * from review where book_id = #{bookId}
    </select>

    <select id="getBookRatingStatistic" resultType="com.bookmarket.dto.RatingCountDTO">
        select rating , count(*) count
        from review
        where book_id = #{bookId}
        group by rating;
    </select>

</mapper>
