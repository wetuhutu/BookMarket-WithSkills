<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookmarket.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.bookmarket.pojo.Order">
            <id property="id" column="id" />
            <result property="orderNo" column="order_no" />
            <result property="buyerId" column="buyer_id" />
            <result property="sellerId" column="seller_id" />
            <result property="bookId" column="book_id" />
            <result property="quantity" column="quantity" />
            <result property="price" column="price" />
            <result property="totalPrice" column="total_price" />
            <result property="status" column="status" />
            <result property="paymentStatus" column="payment_status" />
            <result property="shippingStatus" column="shipping_status" />
            <result property="createdAt" column="created_at" />
            <result property="paidAt" column="paid_at" />
            <result property="shippedAt" column="shipped_at" />
            <result property="receivedAt" column="received_at" />
    </resultMap>

    <insert id="save" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO `order` (
            order_no,
            buyer_id,
            seller_id,
            book_id,
            book_title,
            price,
            quantity,
            total_price
            ) VALUES (
                     #{orderNo},
                     #{buyerId},
                     #{sellerId},
                     #{bookId},
                     #{bookTitle},
                     #{price},
                     #{quantity},
                     #{totalPrice}
                 )
    </insert>

    <update id="updateOrder">
        update `order`
        set status          = 'received',
            shipping_status = 'received',
            received_at     = now()
        where id = #{orderId}
          and received_at IS NULL;
    </update>

    <update id="cancelOrder">
        update `order`
        set status = 'cancelled',
            cancelled_at = now()
        where id = #{orderId} and status != 'received'
    </update>

    <select id="getOrdersByUserId" resultType="com.bookmarket.pojo.Order">
        select * ,
               book.title bookTitle,
               book.cover bookCover,
               user.username sellerName
               from `order` o
                   join book on o.book_id = book.id
                   join user on o.seller_id = user.id
                 <where>
                    o.buyer_id = #{userId}
                    <if test="status!=null and status!= '' ">and o.status = #{status}</if>
                 </where>
    </select>
    <select id="getOrderById" resultType="com.bookmarket.pojo.Order">
        select * from `order` where id = #{orderId}
    </select>


</mapper>
